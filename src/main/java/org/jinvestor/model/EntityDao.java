package org.jinvestor.model;

/**
 *
 * @author Adam
 */
public class EntityDao {
// TODO (AF) using EntityUtils, maybe it is actually entity utils
	// TODO (AF) BarDao = EntityDaoFactory.get(Bar.class);
}
